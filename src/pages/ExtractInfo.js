import React from 'react';
import { Box, Typography } from '@mui/material';

const ExtractInfo = () => (
  <Box sx={{ p: 3 }}>
    <Typography variant="h4">Extract Info</Typography>
    <Typography variant="body1" sx={{ mt: 2 }}>
      This is the Extract Info page. Coming soon!
    </Typography>
  </Box>
);

export default ExtractInfo;
